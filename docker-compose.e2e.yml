services:
  redis:
    container_name: chipin-e2e-redis
    ports: !override
      - "16379:6379"
      - "18001:8001"
    volumes: !override
      - e2e-redis-data:/data

  app:
    container_name: chipin-e2e-app
    ports: !override
      - "18080:80"

volumes:
  e2e-redis-data:
